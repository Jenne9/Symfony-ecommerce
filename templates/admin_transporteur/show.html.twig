{% extends 'base-backend.html.twig' %}

{% block title %}Transporteur{% endblock %}

{% block body %}
    <h1>Transporteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transporteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ transporteur.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ transporteur.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ transporteur.prix }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ transporteur.createdAt ? transporteur.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ transporteur.updatedAt ? transporteur.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_transporteur_index') }}">back to list</a>

    <a href="{{ path('app_admin_transporteur_edit', {'id': transporteur.id}) }}">edit</a>

    {{ include('admin_transporteur/_delete_form.html.twig') }}
{% endblock %}
