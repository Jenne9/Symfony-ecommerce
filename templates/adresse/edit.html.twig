{% extends 'base.html.twig' %}

{% block title %}Modifier adresse{% endblock %}

{% block body %}
<div class="login_register_wrap section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-6 col-md-10 my-5">
                    <div class="bg-white px-5">
                        <h3 class="text-dark text-center py-2">Modifier l'adresse</h3>
                        <div class="content">
                            {{ include('adresse/_form.html.twig', {'button_label': 'Modifier'}) }}
                            {{ include('adresse/_delete_form.html.twig') }}
                            <div class="d-grid gap-2 ">
                                <button class="btn btn-warning mx-5 mt-2 mb-5" type="button"><a class="nav-link"href="{{ path('app_user') }}">Mon Compte</a></button>                    
                            </div>
                        </div>                          
                    </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
