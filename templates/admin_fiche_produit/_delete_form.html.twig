<form method="post" action="{{ path('app_admin_fiche_produit_delete', {'id': fiche_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_produit.id) }}">
    <button class="btn">Delete</button>
</form>
